pipeline {
    agent any
    stages {
        stage('Maven build、test') {
            steps {
                sh "mvn --version"
            }
        }
        stage('docker compose') {
            steps {
                echo "运行成功"
            }
        }
    }
}